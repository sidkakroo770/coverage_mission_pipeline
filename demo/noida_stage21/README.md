# Noida Stage 21 audited demo

This directory contains the minimal inputs, generated ArduPilot missions, execution scripts and evidence required by `coverage-swarm simulate-existing`.

The demo is intentionally immutable. Generate new scenarios in separate directories rather than editing these audited mission files.
